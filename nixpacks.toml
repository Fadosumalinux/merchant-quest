[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["cd client && npm install && npm run build && cd ../server && npm install"]

[start]
cmd = "cd server && npx prisma generate && npx prisma db push && npx prisma db seed && npx tsx src/index.ts"
