{
  "name": "merchant-quest",
  "private": true,
  "scripts": {
    "setup": "cd server && npm install && cd ../client && npm install && cd ../server && cp .env.example .env && npx prisma db push && npx prisma db seed",
    "build": "cd client && npm run build",
    "dev": "cd server && npm run dev",
    "start": "cd server && npm run dev",
    "full": "npm run setup && npm run dev"
  }
}
