{
  "name": "merchant-quest",
  "private": true,
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"cd server && npm run dev\" \"cd client && npm run dev\"",
    "install:all": "cd server && npm install && cd ../client && npm install",
    "db:setup": "cd server && cp .env.example .env && npx prisma db push && npx prisma db seed",
    "start": "npm run db:setup && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
